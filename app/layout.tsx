import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Paradise Ranch Wheeler Lake | Horseback Riding in Alabama',
  description: 'Experience scenic horseback riding along Wheeler Lake in Hillsboro, Alabama. Trail rides, guided tours, and family-friendly adventures at Paradise Ranch.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
